[theme]
base = "dark"

# Accent — muted evergreen
primaryColor = "#2F7D6D"

# App background (green-tinted near-black)
backgroundColor = "#0D1412"

# Panels / cards
secondaryBackgroundColor = "#16201C"

# Text
textColor = "#E6F0EC"

font = "sans serif"

[client]
toolbarMode = "minimal"

[browser]
gatherUsageStats = false